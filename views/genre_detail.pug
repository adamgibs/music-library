extends layout

block content

  h1 Genre: #{genre.name}
  
  div(style='margin-left:20px;margin-top:20px')

    h4 Titles
    
    dl
      each title in genre_titles
        dt 
          a(href=title.url) #{title.name}
        dd #{title.summary}

      else
        p This genre has no titles