extends layout

block content
  h1= title
  
  input#myInput(type="text" onkeyup="filterList()" placeholder="Search for genres..")
  ul#myUL
    each genre in genre_list
      li 
        a(href=genre.url) #{genre.name} 
       

    else
      li There are no genres.